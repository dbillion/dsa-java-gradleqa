package dsa;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

/** Placeholder test — ensures the Gradle + JUnit harness is wired correctly. */
class SmokeTest {
    @Test
    void harnessWorks() {
        assertEquals(2, 1 + 1);
    }
}
